export class Emoji {
  public static toEntity(segment: string): string {
    // Placeholder for emoji to entity conversion
    return segment;
  }

  public static toEmoji(segment: string): string {
    // Placeholder for entity to emoji conversion
    return segment;
  }
}